# Generated by DevOps Control Plane — Compose Wizard
services:
  core:
    build:
      context: ./src/core
      dockerfile: Dockerfile
    ports:
    - 8000:8000
    restart: unless-stopped
    command: python -m src.main
  adapters:
    build:
      context: ./src/adapters
      dockerfile: Dockerfile
    ports:
    - 8001:8001
    restart: unless-stopped
    command: python -m src.main
  cli:
    build:
      context: ./src/ui/cli
      dockerfile: Dockerfile
    ports:
    - 8002:8002
    restart: unless-stopped
    command: python -m src.main
  web:
    build:
      context: ./src/ui/web
      dockerfile: Dockerfile
    ports:
    - 8003:8003
    restart: unless-stopped
    command: python -m src.main
  mysql:
    image: mysql:8
    ports:
    - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: changeme
      MYSQL_DATABASE: app
      MYSQL_USER: app
      MYSQL_PASSWORD: changeme
    restart: unless-stopped
