// Auto-generated by DevOps Control Plane — do not edit directly.
// Modify via the Pages segment configuration UI.

import { themes as prismThemes } from 'prism-react-renderer';
import type { Config } from '@docusaurus/types';
import type * as Preset from '@docusaurus/preset-classic';

const config: Config = {
  title: '__SITE_TITLE__',
  tagline: '__SITE_TAGLINE__',
  favicon: 'img/favicon.ico',
  url: '__SITE_URL__',
  baseUrl: '__BASE_URL__',
  onBrokenLinks: 'warn',
  onBrokenMarkdownLinks: 'warn',

// __IF_FEATURE_faster__
  future: {
    experimental_faster: true,
    v4: {
      removeLegacyPostBuildHeadAttribute: true,
    },
  },
// __ENDIF__

// __IF_FEATURE_mermaid__
  markdown: {
    mermaid: true,
  },
// __ENDIF__

  themes: [
// __IF_FEATURE_mermaid__
    '@docusaurus/theme-mermaid',
// __ENDIF__
// __IF_FEATURE_search__
    [
      require.resolve('@easyops-cn/docusaurus-search-local'),
      {
        hashed: true,
        indexDocs: true,
        indexBlog: false,
        indexPages: false,
        docsRouteBasePath: '/',
        language: ['en'],
        highlightSearchTermsOnTargetPage: true,
        searchResultLimits: 8,
        searchBarShortcutHint: false,
      },
    ],
// __ENDIF__
  ],

// __IF_FEATURE_google_translate__
  // Blocking script: set translation-active before page renders (hides main content)
  headTags: [
    {
      tagName: 'script',
      attributes: {},
      innerHTML: '(function(){var c=document.cookie;if(c.indexOf("googtrans=")!==-1&&c.indexOf("googtrans=/en/en")===-1){document.documentElement.classList.add("translation-active")}})();',
    },
  ],
// __ENDIF__

  i18n: {
    defaultLocale: 'en',
    locales: ['en'],
  },

  presets: [
    [
      'classic',
      {
        docs: {
          sidebarPath: './sidebars.ts',
          routeBasePath: '/',
          // __SLOT_REMARK_PLUGINS__
          // __SLOT_REHYPE_PLUGINS__
          // __SLOT_BEFORE_REMARK_PLUGINS__
        },
        blog: false,
        theme: {
          customCss: './src/css/custom.css',
        },
      } satisfies Preset.Options,
    ],
  ],

// __IF_FEATURE_pwa__
  plugins: [
    ...(process.env.SKIP_PWA === '1' ? [] : [[
      '@docusaurus/plugin-pwa',
      {
        debug: false,
        offlineModeActivationStrategies: [
          'appInstalled',
          'standalone',
          'queryString',
        ],
        pwaHead: [
          { tagName: 'link', rel: 'icon', href: '/img/favicon.ico' },
          { tagName: 'link', rel: 'manifest', href: '/manifest.json' },
          { tagName: 'meta', name: 'theme-color', content: '__THEME_COLOR__' },
          { tagName: 'meta', name: 'apple-mobile-web-app-capable', content: 'yes' },
          { tagName: 'meta', name: 'apple-mobile-web-app-status-bar-style', content: 'black-translucent' },
        ],
      },
    ] as const]),
  ],
// __ENDIF__

  themeConfig: {
    navbar: {
      title: '__SITE_TITLE__',
      items: [
// __IF_FEATURE_github__
        {
          href: '__REPO_URL__',
          label: 'GitHub',
          position: 'right',
        },
// __ENDIF__
// __IF_FEATURE_google_translate__
        {
          type: 'html',
          position: 'right',
          value: '<div id="navbar-language-selector"></div>',
        },
// __ENDIF__
// __SLOT_NAVBAR_ITEMS__
      ],
    },
    colorMode: {
// __IF_FEATURE_dark_mode__
      defaultMode: 'dark',
// __ENDIF__
// __IF_NOT_FEATURE_dark_mode__
      defaultMode: 'light',
// __ENDIF__
      disableSwitch: false,
      respectPrefersColorScheme: false,
    },
// __IF_FEATURE_prism_extra__
    prism: {
      theme: prismThemes.__PRISM_LIGHT_THEME__,
      darkTheme: prismThemes.__PRISM_DARK_THEME__,
      additionalLanguages: [
        'markup-templating',
        'java',
        'csharp',
        'ruby',
        'php',
        'rust',
        'go',
        'bash',
        'sql',
        'yaml',
        'json',
        'protobuf',
      ],
    },
// __ENDIF__
    footer: {
      style: 'dark',
      copyright: '__FOOTER_COPYRIGHT__',
    },
  } satisfies Preset.ThemeConfig,
};

export default config;
