import React from 'react';
import { useBuildHashCheck } from './hooks/useBuildHashCheck';
import { useTabMesh } from './hooks/useTabMesh';
// __IF_FEATURE_google_translate__
import LanguageSelector from '@site/src/components/LanguageSelector';
import { useTranslationSystem } from './hooks/useTranslationSystem';
// __ENDIF__
// __IF_FEATURE_peek__
import { usePeekLinks } from './hooks/usePeekLinks';
// __ENDIF__

const BUILD_HASH = '__BUILD_HASH__';

export default function Root({ children }: { children: React.ReactNode }) {
  useBuildHashCheck(BUILD_HASH);
  useTabMesh();
// __IF_FEATURE_google_translate__
  useTranslationSystem();
// __ENDIF__
// __IF_FEATURE_peek__
  usePeekLinks();
// __ENDIF__

// __IF_FEATURE_google_translate__
  return (
    <>
      {children}
      <LanguageSelector />
      <div id="google_translate_hidden" style={{ display: 'none' }} />
    </>
  );
// __ENDIF__
// __IF_NOT_FEATURE_google_translate__
  return <>{children}</>;
// __ENDIF__
}
