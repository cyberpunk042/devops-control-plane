# Python — final entries for 700 total

- id: python.stdlib.typing_clear_overloads
  language: python
  feature_name: "typing.clear_overloads()"
  introduced: "3.11"
  category: typing
  direction: downgrade
  severity: warning
  error_type: runtime_error
  tags: [typing, overloads]
  detection:
    primary: { ast_type: ImportFrom, match: { module: typing, names_contains: clear_overloads } }
  fix: { strategy: manual, manual_instructions: "Not available on older Python." }
  verification: { re_detect: true }
  test: { before: "from typing import clear_overloads\n", after: "# Not available\n" }

- id: python.stdlib.typing_get_overloads
  language: python
  feature_name: "typing.get_overloads()"
  introduced: "3.11"
  category: typing
  direction: downgrade
  severity: warning
  error_type: runtime_error
  tags: [typing, overloads, get]
  detection:
    primary: { ast_type: ImportFrom, match: { module: typing, names_contains: get_overloads } }
  fix: { strategy: manual, manual_instructions: "Not available on older Python." }
  verification: { re_detect: true }
  test: { before: "from typing import get_overloads\n", after: "# Not available\n" }

- id: python.stdlib.sys_intern
  language: python
  feature_name: "sys.intern() usage"
  introduced: "3.0"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [sys, intern, string]
  detection:
    primary: { ast_type: Attribute, match: { value_id: sys, attr: intern } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "sys.intern(s)\n", after: "sys.intern(s)\n" }

- id: python.stdlib.typing_dataclass_transform_313
  language: python
  feature_name: "dataclass_transform improvements"
  introduced: "3.12"
  category: typing
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [typing, dataclass_transform]
  detection:
    primary: { ast_type: ImportFrom, match: { module: typing, names_contains: dataclass_transform } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from typing import dataclass_transform\n", after: "from typing import dataclass_transform\n" }

- id: python.stdlib.collections_abc_buffer
  language: python
  feature_name: "collections.abc.Buffer"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: error
  error_type: import_error
  error_subtype: missing_name
  tags: [collections, abc, buffer]
  detection:
    primary: { ast_type: ImportFrom, match: { module: collections.abc, names_contains: Buffer } }
  fix: { strategy: manual, manual_instructions: "Use typing_extensions or Protocol." }
  verification: { re_detect: true }
  test: { before: "from collections.abc import Buffer\n", after: "# Use Protocol\n" }

- id: python.stdlib.typing_type_param_syntax
  language: python
  feature_name: "type parameter syntax [T]"
  introduced: "3.12"
  category: syntax
  direction: downgrade
  severity: error
  error_type: syntax_error
  tags: [typing, type_param, syntax]
  detection:
    primary: { ast_type: FunctionDef, match: { has_type_params: true } }
  fix: { strategy: manual, manual_instructions: "Use TypeVar explicitly." }
  verification: { re_detect: false }
  test: { before: "def first[T](items: list[T]) -> T: return items[0]\n", after: "T = TypeVar('T')\ndef first(items: list) -> Any: return items[0]\n" }

- id: python.stdlib.inspect_markcoroutinefunction
  language: python
  feature_name: "inspect.markcoroutinefunction()"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: error
  error_type: runtime_error
  error_subtype: missing_name
  tags: [inspect, coroutine, marker]
  detection:
    primary: { ast_type: Attribute, match: { value_id: inspect, attr: markcoroutinefunction } }
  fix: { strategy: manual, manual_instructions: "Not available on older Python." }
  verification: { re_detect: true }
  test: { before: "inspect.markcoroutinefunction(func)\n", after: "# Not available\n" }

- id: python.stdlib.contextlib_suppress_star
  language: python
  feature_name: "contextlib.suppress with ExceptionGroup"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [contextlib, suppress, exceptiongroup]
  detection:
    primary: { ast_type: ImportFrom, match: { module: contextlib, names_contains: suppress } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from contextlib import suppress\n", after: "from contextlib import suppress\n" }

- id: python.stdlib.os_path_splitroot
  language: python
  feature_name: "os.path.splitroot()"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: error
  error_type: runtime_error
  error_subtype: missing_name
  tags: [os, path, splitroot]
  detection:
    primary: { ast_type: Attribute, match: { attr: splitroot } }
  fix: { strategy: manual, manual_instructions: "Use os.path.splitdrive + manual root extraction." }
  verification: { re_detect: true }
  test: { before: "drive, root, tail = os.path.splitroot(path)\n", after: "drive, tail = os.path.splitdrive(path)\n" }

- id: python.stdlib.asyncio_cancel_msg
  language: python
  feature_name: "Task.cancel(msg=)"
  introduced: "3.9"
  category: stdlib
  direction: downgrade
  severity: warning
  error_type: runtime_error
  tags: [asyncio, cancel, message]
  detection:
    primary: { ast_type: Call, match: { func_attr: cancel } }
  fix: { strategy: manual, manual_instructions: "Remove msg parameter from cancel()." }
  verification: { re_detect: false }
  test: { before: "task.cancel(msg='timeout')\n", after: "task.cancel()\n" }
