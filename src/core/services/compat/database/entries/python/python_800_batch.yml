# Python batch — 800

- id: python.stdlib.typing_typevartuple_312
  language: python
  feature_name: "TypeVarTuple unpack syntax"
  introduced: "3.11"
  category: typing
  direction: downgrade
  severity: warning
  error_type: import_error
  tags: [typing, typevartuple, unpack]
  detection: { primary: { ast_type: ImportFrom, match: { module: typing, names_contains: TypeVarTuple } }, exclude: [{ context: type_checking_block }] }
  fix: { strategy: replace_import, transforms: [{ type: replace_import_name, find: { import_module: typing, import_name: TypeVarTuple }, replace: { import_name: "" } }, { type: add_import, import_statement: "from typing_extensions import TypeVarTuple", condition: not_already_present }], backport: { package: typing-extensions } }
  verification: { re_detect: true }
  test: { before: "from typing import TypeVarTuple\n", after: "from typing_extensions import TypeVarTuple\n" }

- id: python.stdlib.collections_abc_callable
  language: python
  feature_name: "collections.abc.Callable"
  introduced: "3.9"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [collections, abc, callable]
  detection: { primary: { ast_type: ImportFrom, match: { module: collections.abc, names_contains: Callable } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from collections.abc import Callable\n", after: "from collections.abc import Callable\n" }

- id: python.stdlib.random_choices
  language: python
  feature_name: "random.choices()"
  introduced: "3.6"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: runtime_error
  tags: [random, choices, weighted]
  detection: { primary: { ast_type: Attribute, match: { value_id: random, attr: choices } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "random.choices(pop, weights=w, k=3)\n", after: "random.choices(pop, weights=w, k=3)\n" }

- id: python.stdlib.dataclasses_asdict_312
  language: python
  feature_name: "dataclasses.asdict improvements"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [dataclasses, asdict]
  detection: { primary: { ast_type: ImportFrom, match: { module: dataclasses, names_contains: asdict } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from dataclasses import asdict\n", after: "from dataclasses import asdict\n" }

- id: python.stdlib.functools_reduce
  language: python
  feature_name: "functools.reduce improvements"
  introduced: "3.8"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [functools, reduce]
  detection: { primary: { ast_type: ImportFrom, match: { module: functools, names_contains: reduce } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from functools import reduce\n", after: "from functools import reduce\n" }

- id: python.stdlib.re_noflag
  language: python
  feature_name: "re.NOFLAG"
  introduced: "3.11"
  category: stdlib
  direction: downgrade
  severity: error
  error_type: runtime_error
  error_subtype: missing_name
  tags: [re, noflag, constant]
  detection: { primary: { ast_type: Attribute, match: { value_id: re, attr: NOFLAG } } }
  fix: { strategy: manual, manual_instructions: "Use 0 instead of re.NOFLAG." }
  verification: { re_detect: true }
  test: { before: "re.compile(pattern, re.NOFLAG)\n", after: "re.compile(pattern, 0)\n" }

- id: python.stdlib.datetime_utcnow_deprecated
  language: python
  feature_name: "datetime.utcnow() deprecation"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: warning
  error_type: deprecation_warning
  tags: [datetime, utcnow, deprecated]
  detection: { primary: { ast_type: Attribute, match: { attr: utcnow } } }
  fix: { strategy: manual, manual_instructions: "Use datetime.now(timezone.utc) instead." }
  verification: { re_detect: true }
  test: { before: "datetime.utcnow()\n", after: "datetime.now(timezone.utc)\n" }

- id: python.stdlib.os_eventfd
  language: python
  feature_name: "os.eventfd()"
  introduced: "3.10"
  category: stdlib
  direction: downgrade
  severity: error
  error_type: runtime_error
  error_subtype: missing_name
  tags: [os, eventfd, linux]
  detection: { primary: { ast_type: Attribute, match: { value_id: os, attr: eventfd } } }
  fix: { strategy: manual, manual_instructions: "Use threading.Event or os.pipe." }
  verification: { re_detect: true }
  test: { before: "fd = os.eventfd(0)\n", after: "r, w = os.pipe()\n" }

- id: python.stdlib.hashlib_sha3
  language: python
  feature_name: "hashlib SHA-3"
  introduced: "3.6"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: runtime_error
  tags: [hashlib, sha3, crypto]
  detection: { primary: { ast_type: Attribute, match: { value_id: hashlib, attr: sha3_256 } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "hashlib.sha3_256(data)\n", after: "hashlib.sha3_256(data)\n" }

- id: python.stdlib.ssl_hostname_checks
  language: python
  feature_name: "ssl hostname checking"
  introduced: "3.10"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [ssl, hostname, security]
  detection: { primary: { ast_type: Import, match: { names_contains: ssl } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "import ssl\n", after: "import ssl\n" }

- id: python.stdlib.sqlite3_default_adapters
  language: python
  feature_name: "sqlite3 deprecated default adapters"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: deprecation_warning
  tags: [sqlite3, adapters, deprecated]
  detection: { primary: { ast_type: Import, match: { names_contains: sqlite3 } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "import sqlite3\n", after: "import sqlite3\n" }

- id: python.stdlib.xml_sax_improvements
  language: python
  feature_name: "xml.sax improvements"
  introduced: "3.8"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [xml, sax, parser]
  detection: { primary: { ast_type: ImportFrom, match: { module: xml.sax } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from xml.sax import parse\n", after: "from xml.sax import parse\n" }

- id: python.stdlib.wsgiref_improvements
  language: python
  feature_name: "wsgiref improvements"
  introduced: "3.11"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [wsgiref, wsgi]
  detection: { primary: { ast_type: ImportFrom, match: { module: wsgiref.simple_server } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from wsgiref.simple_server import make_server\n", after: "from wsgiref.simple_server import make_server\n" }

- id: python.stdlib.http_improvements_312
  language: python
  feature_name: "http module improvements"
  introduced: "3.12"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [http, improvements]
  detection: { primary: { ast_type: ImportFrom, match: { module: http } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "from http import HTTPStatus\n", after: "from http import HTTPStatus\n" }

- id: python.stdlib.selectors_improvements
  language: python
  feature_name: "selectors improvements"
  introduced: "3.10"
  category: stdlib
  direction: downgrade
  severity: info
  error_type: behavioral_change
  tags: [selectors, io, multiplexing]
  detection: { primary: { ast_type: Import, match: { names_contains: selectors } } }
  fix: { strategy: no_fix_needed }
  verification: { re_detect: false }
  test: { before: "import selectors\n", after: "import selectors\n" }
