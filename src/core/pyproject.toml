[project]
name = "core"
version = "0.1.0"
description = "Domain models, services, and engine"
requires-python = ">=3.8"
dependencies = [
    "flask>=3.0.3",
    "pydantic>=2.10.6",
    "pytest>=8.3.5",
]
